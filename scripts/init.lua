-- spec = LocationSpec.new()
-- spec:setType(LocationType.ZERO)
-- Random.seed(seed)
-- spec:setGenFunc(
--   function(location)
--     location.cells = utils.fill(HEIGHT, WIDTH, CellType.UNKNOWN)
--     room = utils.makeRoom(10, 16, 5, 15, CellType.FLOOR)
--     location:addRoom(room, 40, 40)
--     utils.fixOverlapped(location)
--     --utils.placeTemplateInRoom(location, templates.TORCHES)
--     utils.placeTemplateInRoom(location, templates.RIVER)
--     --utils.placeTemplateInRoom(location, templates.LAKE)
--    -- utils.placeTemplateInRoom(location, templates.BLOOD_FIELD)
--     utils.placeWalls(location)
--   end
-- )
--genLocation(seed, spec)

-- CellType.GROUND = CellSpec.new("CELL_GROUND", true, true);

genLocation()















